// Generated by CoffeeScript 1.6.1
(function() {

  $(function() {
    $('#rechercheInternet').click(function() {
      return $.getJSON('https://www.googleapis.com/books/v1/volumes?q=isbn:' + $("#isbn").val(), function(data) {
        if (data.totalItems === 0) {
          alert("Il n'y a pas de livre associé à l'ISBN saisi");
        }
        $("#titre").val(data.items[0].volumeInfo.title);
        $("#description").val(data.items[0].volumeInfo.description);
        $("#auteur").val(data.items[0].volumeInfo.authors);
        $("#langue").val(data.items[0].volumeInfo.language);
        return $("#publication").val(data.items[0].volumeInfo.publishedDate);
      });
    });
    bootbox.setDefaults({
      locale: "fr"
    });
    return $('#confirm').click(function(e) {
      var targetUrl;
      e.preventDefault();
      targetUrl = $(this).attr("href");
      console.log(targetUrl);
      return bootbox.confirm("Êtes-vous sûr ?", function(result) {
        if (result) {
          return window.location.href = targetUrl;
        } else {
          return $(this).modal('hide');
        }
      });
    });
  });

}).call(this);
