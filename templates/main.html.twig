<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="fr" lang="fr">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>BibliApp</title>
		<link href={{url_css}} rel="stylesheet" type="text/css" />
	</head>
	<body>
		<header>
			<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
				<div class="container">
					<div class="navbar-header col-md-12 col-sm-12 col-xs-12">
					  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu">
						<span class="sr-only">Toggle navigation</span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					  </button>
					  <a class="navbar-brand" href="{{url_base}}">BibliApp Engine</a>
					  <span class="pull-right label">
						Bienvenue {{user[0].prenom}}
						
						<div class="btn-group btn-group-xs">
							<a href="{{url_base}}user/emprunt" class="btn btn-default"> Profil / Mes emprunts <span class="badge">{{user[0].nbEmprunt}}</span></a>
							{% if user[0].droit == "A" %}
								<a href="{{url_base}}admin" class="btn btn-default">Administration</a>
							{% endif %}
							<a href="?logout" class="btn btn-default">Déconnexion</a>
						</div>

						</span>
					</div>
					<div class="collapse navbar-collapse" id="menu">
						<div class="col-md-12 col-sm-12 col-xs-12">
							<form class="navbar-search" action="{{url_base}}recherche" method="get" name="formRecherche">
								<span class="col-md-7 col-sm-7 col-xs-12">
								<span class="label">Titre/Mot-Clé/ISBN :</span>
								<input id="recherche" class="search-query" placeholder="recherche" name="text" list="urldata">
								<datalist id="urldata">
									{% for titre in titres  %}
										<option value="{{titre}}"></option>
									{% endfor %}
									{% for discipline in disciplines %}
									<option value="{{discipline.nom}}" ></option>
									{% endfor %}
								</datalist>
								<input type="submit" value="Rechercher">
								<a href="{{url_base}}rechercheAv">Recherche Avancé</a>
								</span>
								<span class="col-md-5 col-sm-5 col-xs-12">
								<span class="label">Trier par :</span>
								<select name="tri">
									<option value="1">Année</option>
									<option value="2">Date ajout</option>
									<option value="3">Alphabétique</option>
								</select>
								<input type="checkbox" name="mode" value="true">
								<span class="label">Ordre inversé</span>
								</span>
							</form>
						</div>

					</div><!--/.nav-collapse -->
				</div>
			</div>
		</header>

		<div class="container">
			<div class="starter-template col-md-12">
			
			   {% block content %}
				<section>
				</section>
				{% endblock %}
		
			</div>
		</div>
	<footer>
		<script type="text/javascript" src="{{url_base}}js/jquery-2.0.3.min.js"></script>
		<script type="text/javascript" src="{{url_base}}js/dist/src/App.js"></script>
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
		<script src="{{url_base}}/templates/js/bootstrap.min.js"></script>
		<script src="{{url_base}}js/bootbox.min.js"></script>
	</footer>


</body>

</html>
